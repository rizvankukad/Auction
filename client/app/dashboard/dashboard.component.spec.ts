'use strict';

describe('Component: DashboardComponent', function() {
  // load the controller's module
  //beforeEach(module('auctionApp.dashboard'));

  var DashboardComponent;

  it('should ...', function() {
    expect(1).to.equal(1);
  });
});
